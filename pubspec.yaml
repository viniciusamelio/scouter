name: scouter
description: A NestJS based http wrapper, to handle apis development following its code style
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  alfred: ^0.1.5+3
  fpdart: ^0.2.0
  kiwi: ^4.0.2

dev_dependencies:
  faker: ^2.0.0
  lints: ^2.0.0
  test: ^1.16.0
